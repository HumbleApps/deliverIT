import { useEffect } from 'react';
// import { useSelector } from 'react-redux';
// import SplashScreen from 'react-native-splash-screen';

// import { selectIsFetching } from 'selectors/userSelector';

import useNotification from 'hooks/useNotification';
import useCrashlytics from 'hooks/useCrashlytics';

const WithBootstrap = () => {
  // const isFetchingUser = useSelector(selectIsFetching);

  // useNotification();
  // useCrashlytics();

  // useEffect(() => {
  //   if (!isFetchingUser) {
  //     SplashScreen.hide();
  //   }
  // }, [isFetchingUser]);

  return null;
};

export default WithBootstrap;
