import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 66,
    paddingBottom: 20,
  },
  flatlist: {
    paddingHorizontal: 8,
  },
  noNotificationView: {
    paddingTop: 30,
    display: 'flex',
    alignSelf: 'center',
  },
});

export default styles;
