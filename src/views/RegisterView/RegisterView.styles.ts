import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  form: {
    // padding: 20,
    height: '100%',
  },
  input: {
    textAlign: 'left',
    fontWeight: '400',
  },
  radioContainer: {
    flexGrow: 1,
  },
  okBtn: {
    marginBottom: 20,
  },
});

export default styles;
